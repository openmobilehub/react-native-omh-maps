pre-commit:
  parallel: true
  commands:
    lint-android:
      run: cd sample-app/android && ./gradlew detekt -PautoCorrect=true --parallel --continue
    lint-ts:
      glob: '*.{js,ts,jsx,tsx}'
      run: npx eslint {staged_files}
    types:
      glob: '*.{js,ts, jsx, tsx}'
      run: npx tsc-files --noEmit {staged_files}
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
